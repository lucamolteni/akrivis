apiVersion: v1
kind: Service
metadata:
  name: {{ include "scorecards-helm-chart.fullname" . }}
  labels:
    {{- include "scorecards-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scorecards-helm-chart.selectorLabels" . | nindent 4 }}
